<!-- Generated by `spicy-regs-dict generate`. Do not edit by hand. Edit data_dictionary/descriptions.yaml or the schema, then regenerate. -->

# `member_votes`

**Member vote positions**

One row per member's position on one roll call. `position` keeps the publisher's own word (the Clerk spells both Aye/No and Yea/Nay) and `position_normalized` carries the fold, so a count and a quotation are both answerable. All columns are stored as VARCHAR.

**Coverage.** Sampled, and accumulating. Measured on the same cold-start run as `roll_call_votes` (receipt `d1-measured-run-2026-09-19/`): 249,099 member positions across its 577 roll calls, an average of 432 a roll call. One row per member per roll call in `roll_call_votes`, so it inherits that table's House-only scope, per-run cap and skip-what-is-already-held behavior exactly. *(measured 2026-09-19)*

- **Parquet file:** `member_votes.parquet`
- **Queryable via MCP `query_sql`:** Yes

| Column | Type | Description |
| --- | --- | --- |
| `vote_id` | `VARCHAR` | The roll call this position belongs to, as roll_call_votes keys it. |
| `congress` | `VARCHAR` | The numbered Congress the roll call belongs to. |
| `chamber` | `VARCHAR` | house or senate. |
| `session` | `VARCHAR` | The session number within that Congress. |
| `roll_number` | `VARCHAR` | The roll-call number within that session. |
| `member_key` | `VARCHAR` | The file-stated id: `lis:` plus the LIS id on a Senate record, the bare bioguide id on a House one, and `name:` plus the publisher's name where the file states neither. A Senate bioguide comes from the crosswalk and does not always resolve, so keying on it would split one member's votes across two rows; an identity column also cannot be null, which the design's bioguide key would have been. |
| `bioguide_id` | `VARCHAR` | The voting member's bioguide id, where the publisher or the crosswalk supplies one. |
| `lis_id` | `VARCHAR` | The voting member's Senate LIS id, which only the Senate file carries. |
| `member_name` | `VARCHAR` | The member's name exactly as the roll-call source spells it. |
| `party` | `VARCHAR` | The member's party as the roll-call source states it. |
| `state` | `VARCHAR` | The member's state as the roll-call source states it. |
| `position` | `VARCHAR` | The member's position exactly as the publisher spelled it (Yea, Aye, Not Voting...). |
| `position_normalized` | `VARCHAR` | That position folded onto yea, nay, present or not_voting. |
| `vote_date` | `VARCHAR` | The date of the roll call; the merge prefers the larger value. |
