<!-- Generated by `spicy-regs-dict generate`. Do not edit by hand. Edit data_dictionary/descriptions.yaml or the schema, then regenerate. -->

# `amendments`

**Amendments**

One row per amendment, from the Congress.gov amendment list and detail routes. Keyed on the amendment's own identity, not the bill it amends, because an amendment can amend another amendment; `amended_bill_id` and `amended_amendment_id` are foreign keys. All columns are stored as VARCHAR.

**Coverage.** Window: the 119th Congress, complete as of 2026-09-23: 7,095 amendments, equal to the publisher's declared count, each with its detail record. Every cell matches the retained list and detail responses (receipt `amendment-details-2026-09-23/` in the fork execution receipts). Scheduled runs walk an `updateDate` window from the table's watermark, at most ninety days, and fetch detail for each amendment in it. *(measured 2026-09-23)*

**Data quality.** The publisher's `updateDate desc` order repeats and skips amendments across page boundaries when stamps tie, so one walk can serve the declared count while missing some amendments. The builder pools alternating-order passes until the distinct count reaches the declared count. Sponsor, chamber, submitted date and the amended bill or amendment come from each amendment's detail record; the list route carries none of them. 89 House amendments name the Rules Committee as sponsor and have no member sponsor to record. All columns are stored as VARCHAR.

- **Parquet file:** `amendments.parquet`
- **MCP `query_sql` support:** Configured; requires an available artifact.
- **Publication status:** Not established by this schema page or its measurement date.

| Column | Type | Description |
| --- | --- | --- |
| `amendment_id` | `VARCHAR` | Natural key: congress, lowercase amendment type and number joined with hyphens. |
| `congress` | `VARCHAR` | The numbered Congress this amendment belongs to. |
| `amendment_type` | `VARCHAR` | The publisher's amendment type, lowercased (samdt, hamdt, suamdt). |
| `amendment_number` | `VARCHAR` | The amendment's number within its Congress and type. |
| `purpose` | `VARCHAR` | The publisher's stated purpose of the amendment. |
| `description` | `VARCHAR` | The publisher's description of the amendment. |
| `proposed_date` | `VARCHAR` | The date the amendment was proposed, where the publisher states one. |
| `submitted_date` | `VARCHAR` | The date the amendment was submitted, where the publisher states one. |
| `chamber` | `VARCHAR` | The chamber the amendment was offered in. |
| `update_date` | `VARCHAR` | The publisher's updateDate; the merge prefers the larger value. |
| `latest_action_date` | `VARCHAR` | Date of the publisher's latestAction entry on this amendment. |
| `latest_action_text` | `VARCHAR` | Text of the publisher's latestAction entry on this amendment. |
| `sponsor_bioguide_id` | `VARCHAR` | Bioguide id of the first sponsor the publisher lists. |
| `sponsor_full_name` | `VARCHAR` | Full name of that sponsor, exactly as the publisher spells it. |
| `sponsor_party` | `VARCHAR` | The sponsor's party string in full, not a single letter. |
| `amended_bill_id` | `VARCHAR` | Natural key of the bill this amendment amends, where it amends a bill. |
| `amended_amendment_id` | `VARCHAR` | Natural key of the amendment this amendment amends, where it amends one. |
| `url` | `VARCHAR` | The publisher's own URL for this amendment. |
