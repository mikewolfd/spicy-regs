<!-- Generated by `spicy-regs-dict generate`. Do not edit by hand. Edit data_dictionary/descriptions.yaml or the schema, then regenerate. -->

# `bill_publisher_summaries`

**CRS bill summaries (publisher)**

One row per CRS summary the publisher states on a bill, at the version and action it describes. `summary_html` is kept exactly as the publisher escaped it. All columns are stored as VARCHAR.

**Coverage.** Sampled. The adoption run produced 10,806 rows in this output from the 118th Congress's HR and S BILLSTATUS archives, processing 16,213 bills in one pass. The pass used 16 requests (8 keyed, 8 keyless), with the printing cap fixed at four and model calls disabled. Receipt: `rollups-0-24-0-adoption-2026-09-20/`. Local output only; not uploaded. *(measured 2026-09-20)*

- **Parquet file:** `bill_publisher_summaries.parquet`
- **MCP `query_sql` support:** Configured; requires an available artifact.
- **Publication status:** Not established by this schema page or its measurement date.

| Column | Type | Description |
| --- | --- | --- |
| `bill_id` | `VARCHAR` | The bill this summary describes. |
| `summary_version_code` | `VARCHAR` | The publisher's two-digit summary version code (00 introduced, 53 passed House...). |
| `action_date` | `VARCHAR` | The date of the action this summary describes. |
| `action_desc` | `VARCHAR` | The publisher's description of that action. |
| `update_date` | `VARCHAR` | When the publisher last updated this summary; the merge prefers the larger value. |
| `summary_html` | `VARCHAR` | The summary exactly as the publisher escaped it, from either placement, untruncated. |
| `summary_chars` | `VARCHAR` | Character length of summary_html, so a truncation upstream is visible without reading it. |
