<!-- Generated by `spicy-regs-dict generate`. Do not edit by hand. Edit data_dictionary/descriptions.yaml or the schema, then regenerate. -->

# `roll_call_votes`

**House and Senate roll-call votes**

One row per roll call: the publisher's own tally, and the bill it refers to. The four tally columns are the publisher's own counts or they are NULL; they are never zero-filled. All columns are stored as VARCHAR.

**Coverage.** Sampled, and accumulating. Measured on one cold-start run (receipt `d1-measured-run-2026-09-19/`): the `house-vote` index listed 676 House votes for the 119th, 577 of which the linkage rule could index, and all 577 were fetched from the Clerk under the 1,500-per-run cap with no refusal — 4 keyed and 577 keyless requests in 310 seconds, 577 rows produced locally from an empty prior. Local output only; not uploaded. That measured run was House-only; the Senate LIS menu route landed 2026-09-21 (9c58095), so both chambers are now acquired and published, bounded by the same per-run cap on how many roll calls are fetched, newest first. A roll call already published with a tally is not fetched again, so a steady-state run is meant to read only new ones plus the newest few, which are re-read in case the publisher corrected them. **The skip is design, not measurement**: D1 was a cold start, so nothing was held and nothing was skipped, and the saving described here has not been observed yet — the second run is what measures it. Each chamber's own session index is its population: the Senate LIS menu, and the House Clerk's EVS index pages (spicy-docs `list_house_votes`); both hosts are keyless, so the rollup uses no api.data.gov key. A set of roll calls reached from bills alone is not an index of a session's roll calls and never establishes a complete selection. The scheduled scope is the sitting Congress; an earlier Congress is backfilled by dispatching its number, read newest first under the same cap, and once held is never re-read, because the overlap re-read applies to a sitting Congress only. The Clerk and LIS files reach the 101st Congress; the bill family's references reach the 108th. A House roll call that a bill's own action names can be published a cron before the Clerk's index lists it, since the references are read independently and the Clerk file is addressable from the roll-call key alone; such a row carries the same tally and member positions as any other, so this widens timeliness rather than the coverage claim. `vote_date` keeps the Clerk's own spelling (`9-Sep-2025`), which does not sort as a date, so no range over it is quoted here. *(measured 2026-09-19)*

**Data quality.** Two statements link a roll call to a bill, and `match_rule` names the one that did. `bill_action_recorded_vote`: a `recordedVotes` entry on the bill's own action, read from `bill_vote_references`. `vote_file_legislation`: the vote file's own statement of its measure, the Clerk's `legis_num` or the Senate's document (or, on an amendment vote, the document it amends), with the file's own URL as `match_url`. The bill's action wins; where two bills' actions record one roll call, the one the file names wins (the Senate passing a House bill after its Senate twin); with no recorded reference the file links alone. A procedural question, a nomination, a treaty or two different bills link nothing. Measured on the 108th and 119th (receipt `votes-backfill-2026-09-26/file-links/`), the files add 881 links and agree with the recorded reference on 1,803 of the 1,809 roll calls both link; the six others are those Senate passages. `conflict_count` is per roll call, not per run: it counts the later references, the file's included, that named a different bill for *this* roll call, and they are kept rather than dropped. A held row is relinked from its own columns without a fetch; a House row published before `legis_num` existed is read again once to gain it. The contract's `house_vote_legislation` rule (Congress.gov's `house-vote` listing) is no longer produced and no published row carries it: it was retired with the api.data.gov key it needed (2026-09-26). Each run fills a NULL `vote_day` on every held (completely captured) roll call from its own `vote_date` before merging, so a row published before the column existed gains it without a re-fetch. An incomplete capture is not held: it is re-fetched first each run and takes its day from the file. A linkage-only row keeps NULL.

- **Parquet file:** `roll_call_votes.parquet`
- **MCP `query_sql` support:** Configured; requires an available artifact.
- **Publication status:** Not established by this schema page or its measurement date.

| Column | Type | Description |
| --- | --- | --- |
| `vote_id` | `VARCHAR` | Natural key: congress, chamber, session and roll number joined with hyphens. |
| `congress` | `VARCHAR` | The numbered Congress the roll call belongs to. |
| `chamber` | `VARCHAR` | house or senate. |
| `session` | `VARCHAR` | The session number within that Congress. |
| `roll_number` | `VARCHAR` | The roll-call number within that session. |
| `vote_date` | `VARCHAR` | The chamber's literal date; sorts as text, not by time (see `vote_day`). |
| `source_url` | `VARCHAR` | The publisher's own URL for this roll call. |
| `question` | `VARCHAR` | The question put to the chamber, as the publisher states it. |
| `result` | `VARCHAR` | The stated result of the roll call. |
| `yea` | `VARCHAR` | Yea votes, from the Clerk's yea-total or the Senate's yeas. |
| `nay` | `VARCHAR` | Nay votes, from the Clerk's nay-total or the Senate's nays. |
| `present` | `VARCHAR` | Present votes, as each publisher counts them. |
| `not_voting` | `VARCHAR` | Members not voting: the Clerk's not-voting-total, or the Senate's absent, folded to one column. |
| `tallies_json` | `VARCHAR` | Every count the publisher stated, under the publisher's own names, as a JSON object. Candidate elections keep literal choice labels here and leave the four ordinary tally columns NULL. |
| `member_vote_count` | `VARCHAR` | How many member positions the file carried; the member_votes row count for this roll call. |
| `bill_id` | `VARCHAR` | The bill this roll call refers to: from a bill action's recorded-vote reference, a vote-list reference, or the vote file's own statement of its measure. |
| `match_rule` | `VARCHAR` | Which vote-matching rule named that bill: bill_action_recorded_vote (the bill's own action records this roll call), house_vote_legislation (Congress.gov's House vote list), vote_file_legislation (the vote file names the bill: the Clerk's legis-num, or the Senate's document or amended document), or unmatched. |
| `match_action_index` | `VARCHAR` | Position of the action whose recordedVote named this roll call, where one did. |
| `match_url` | `VARCHAR` | The reference URL the match was read from. |
| `conflict_count` | `VARCHAR` | How many later references disagreed with the one that won; kept, never dropped. |
| `tally_kind` | `VARCHAR` | positions for ordinary totals, candidates for native named-choice totals; NULL on legacy or linkage-only rows. |
| `documents_json` | `VARCHAR` | Ordered Senate document objects with native congress, type, number, name, title and short_title; [] for captured votes without documents, NULL for legacy or linkage-only rows. Numbers retain publisher spelling, including nomination suffixes; this does not assert a matched bill or nomination. |
| `amendments_json` | `VARCHAR` | Ordered Senate amendment objects with native number, to_amendment_number, to_amendment_to_amendment_number, to_document_number, to_document_short_title and purpose; [] for captured votes without amendments, NULL for legacy or linkage-only rows. Repeated empty-ID blocks remain separate observations; no document pairing is inferred. |
| `vote_day` | `VARCHAR` | The chamber's own printed vote date as an ISO day (YYYY-MM-DD) in Eastern local time, which sorts where vote_date does not and is never the UTC day of a Congress.gov recordedVotes date; NULL where the file prints no date, on linkage-only rows, and on rows published before this column until a host backfills them. |
| `legis_num` | `VARCHAR` | The Clerk's own legis-num as the House vote file states it (H R 3354, H RES 682, QUORUM); empty when a captured Clerk file states none; NULL on Senate rows, linkage-only rows and House rows captured before this column. |
