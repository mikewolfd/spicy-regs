# Fork output ledger — September 21, 2026

This dated ledger accounts for every declared rollup output plus base regulatory, comments and rulemaking outputs at code `ea64719`. It records 44 producer paths and 82 distinct output keys, including dynamic partition patterns.

The [consolidated backlog](../fork-generation.md) remains the task list. [Machine-readable evidence](/Users/mikewolfd/Work/corpora/fork-execution-2026-09-21/output-ledger.json) records each candidate path, measured size/count/schema, source assessment, scope, required and optional inputs, observed publication pins, next action and receipt. A missing pin or unresolved scope remains a blocker. Existing populations must survive narrower repairs.

- **Pins.** A row records `qualified at <pin> (<date>)` for the generation its audit covered, not a claim that the pin is live; `scripts/check_ledger_pins.py` reads the public index and reports rows whose live generation differs. Withdrawn, unproduced, blocked and known-wrong outputs carry no such phrase, and base objects outside the index (dockets, documents, docket search, comments and its index) record `verified at table digest <digest> (<date>; ETag <etag>)`, whose ETag the script compares with a HEAD of the public object (a row without the ETag is reported unpinned).
- **Names.** A generation is named by the first 8 hex digits of its `artifactDigest`; a table digest is labelled "table"; units are named (rows, distinct keys, pairs).
- **Evidence.** Every load-bearing number cites a retained script or receipt; numbers copied from a publisher's statement cite the receipt.

Public data destination: `https://pub-72e95c0c20a84508b42b03a6ff6d55f8.r2.dev`. Local candidates are not fork publications. Retained qualified generations include FEC, members/terms, nominations, treaties, the five-member report family, repaired dockets/documents, docket search, two document-derived tables, Appropriations press-feed windows, the retained Agenda edition and complete June 30 court clusters. As of 2026-09-23 the following are also source-qualified or validated against raw data:
- **Regulatory:** the Federal Register and its docket links; the rulemaking dataset; the comments table (all 133 comment-bearing agencies, with six plus ACF re-verified and repaired from native source) with its index and the three T15 summaries.
- **Legislative:** laws; committee rosters; amendments with their sponsor and amended-bill detail; the reconciled bill family (decision 1) and its subjects; member vote terms (decision 2); roll-call bill links.
- **Courts and other sources:** the court citation tables and opinion index; CRS reports; FCC proceedings and filings; bounded SAM and lobbying loads (decision 10).

CFR's parts are placed from each volume's `PART` heading since generation `de703ffe…` (see the CFR part ancestry entry in the [execution log](fork-execution-log-2026-09-23.md)). The September 23 parsing survey found join, text and key limits in some qualified tables; each affected row names them. Committee meetings, house communications, record issues, print citations, Senate expenditures and GAO reports were published by scheduled runs and source-qualified at the generations their rows name; the qualification receipts are in `scheduled-published-qualification/` under the execution receipts. Scheduled generations newer than a row's qualified pin await audit (decision 3). Both repositories were pushed on 2026-09-23 and the workflows held for the push were re-enabled that day (decisions record, decisions 11 and 22).

| Task | Producer | Output | Delivery state |
| --- | --- | --- | --- |
| T13 | `run-rollup-court-opinion-clusters` | `court_opinion_clusters.parquet` | generated and verified for complete 2026-06-30 edition; qualified at `7a2cbdb7…` (2026-09-22) |
| T13 | `run-rollup-court-opinion-bodies` | `court_opinion_bodies.parquet` | withdrawn 2026-09-22 and removed 2026-09-23: builder, rollup, workflow and catalog/MCP registration deleted, and the push deleted the fork workflow; text links out through the clusters' `absolute_url` |
| T13 | `run-rollup-court-citations` | `court_citations.parquet`, `court_citation_map.parquet`, `court_parentheticals.parquet` | added and published for the complete 2026-06-30 edition: qualified at `f1e2e523…` (2026-09-22); public row counts verified |
| T13 | `run-rollup-court-opinions` | `court_opinions.parquet` | added and published for the complete 2026-06-30 edition (text-free): qualified at `f7cc67cc…` (2026-09-22); public row counts verified; no scheduled workflow (54.6 GB source) |
| T16 | `run-rollup-bill-subjects` | `bill_subjects.parquet` | published and qualified at `86137cc2…` (2026-09-23): the retained 20,013-bill enrichment (GovInfo BILLSTATUS, enriched 2026-08-22); every bill exists in the reconciled family, its 5,965 BILLSTATUS-bearing bills agree on every policy area and all but one subject set, and 25 random others match raw BILLSTATUS exactly; no scheduled run has started (see Operations). From `b7283e4` (plan P6) the rollup copies BILLSTATUS subjects from the bill family for the 108th Congress on, reads the bulk zips of folders the family has not read, and asks the Congress.gov API only for older Congresses and refused files, under a run deadline and start-to-start pacing |
| T15 | `run-rollup-feed-summary` | `feed_summary.parquet` | published 2026-09-23 as `1a833d52…`, rebuilt on the ACF parent as `4c2e2cd9…` (every comment count and document date a direct recount from the parents) and qualified at `0caab1d2…` (2026-09-23): 279,124 dockets, cell for cell equal to `4c2e2cd9…` on unchanged parents (only row order differs) (receipt `drift-audit-2026-09-23/`) |
| T15 | `run-rollup-agency-stats` | `agency_stats.parquet` | published 2026-09-23 as `d4fa809a…`, rebuilt on the ACF parent as `a9b3d6fc…` (every count a direct recount) and qualified at `b3d816c0…` (2026-09-23): 316 agencies; table bytes equal `a9b3d6fc…`'s (receipt `drift-audit-2026-09-23/`) |
| T15 | `run-rollup-agency-monthly-volume` | `agency_monthly_volume.parquet` | generated and verified; qualified at `5106061c…` (2026-09-23): 77,934 rows equal row for row to `bb0d6255…` (only row order differs; footer input `7907ebe3…`), which equalled two separate re-derivations from `documents.parquet` table `7907ebe3…` (1,999,503 dated rows; 2,020 null and 8 year-zero dates omitted) (receipts `derived-requalification-2026-09-23/`, `drift-audit-2026-09-23/`) |
| T15 | `run-rollup-docket-search` | `docket_search.json.gz` | generated and verified at table digest `230e55ae…` (2026-09-21) |
| T15 | `run-rollup-lifecycles` | `rulemaking_lifecycles.parquet` | withdrawn 2026-09-23 (decision 4): the 2026-09-22 scheduled run had published generation `2f001194…` (26,519 rows); the family was conditionally removed from the index, the workflow disabled and unscheduled; pairing semantics remain blocked |
| T15 | `run-rollup-discovery-signals` | `discovery_signals.parquet` | generated and verified; qualified at `e17fe02f…` (2026-09-23), the UTC rule of `31dadbe5…` carried forward: its 6 rows equal a separate re-derivation from `documents.parquet` table `7907ebe3…` at its recorded `as_of` (2026-09-23 23:23:04 UTC), as `e3f835b8…`'s 9 rows did at theirs; the change is the moving window. No parent row lies between the UTC and New York readings of any window boundary, so the data does not exercise the rule; it rests on the recorded date policy (receipt `derived-requalification-2026-09-23/ds-rederive-e17fe02f.json`) |
| T15 | `run-rollup-fr-docket-links` | `fr_docket_links.parquet` | rebuilt by schedule on `cf89be63…` and qualified at `9bbeada2…` (2026-09-23): its 899,377 rows equal a separate re-derivation from that parent row for row in both directions; against `92f99b00…` (itself equal to a re-derivation from `731984ca…`) it adds 150 rows for 76 of the 98 documents of the 2026-09-23 issue and updates one (2026-19147) (receipt `rulemaking-joins-2026-09-23/candidate-0.31.0/links-qualification.json`) |
| T11 | `run-rollup-cfr-sections` | `cfr_sections.parquet` | parts placed from each volume's `PART` heading since `de703ffe…` (2026-09-23): 319,501 rows, `part` changed on exactly 9,250 rows against `8fb97150…` (title 41 4,732, title 43 3,018, title 14 vol 4 1,492, 8 typo and range rows), `section` on 13,955 and `cfr_ref` on 10,886, every other column byte-equal, eCFR's 2025-01-01 structure agreeing on every compared row of titles 41, 43 and 14 and GovInfo MODS on 4,517 of 4,518 changed rows (receipt `cfr-ancestry-fix-2026-09-23/`). Republished by schedule as `f6192c07…` (2026-09-23): 321,010 rows; the new `CFR-2026-title9-vol1` adds 1,509 granules (GovInfo's declared count), each placed under its volume's heading; the publisher re-stamped `CFR-2025-title9-vol1`, whose 1,510 rows change only `last_modified`; the other 317,991 rows are identical to `de703ffe…`; 10 added rows match GovInfo (receipt `drift-audit-2026-09-23/`). A `replace_all` run on SpicyDocs 0.31.0 (run 35940291235) published `08e585dd…`, qualified at `08e585dd…` (2026-09-23): 321,010 rows, every cell equal to `f6192c07…` except `cfr_ref` on 2 rows (37 CFR 2.188, 7 CFR 760.2210) that regain their citation now a single section printed with `§§` reads as one; the candidate scan's earlier count of 12 was a range-flag count, not a count of missing citations. `cfr_ref` is the printed citation (ruling 5) and is NULL on 7,910 range, parenthesized and typo rows; title 41's compound-part citations agree with the citation grammar's key (receipt `cfr-replace-all-2026-09-23/`) |
| T16 | `run-rollup-congress-bills` (retired, A1) | `congress_bills.parquet` | the narrow writer's table is the reconciled family's `congress_bills` (qualified at `a846cb44…` (2026-09-23); see T09). Its run 35915872100 published `c789aaa9…`, not qualified: it replaced 3,044 `update_date` instants with same-day dates and 3,095 congress.gov page URLs with API resource URLs, and its list walk skipped 13 in-window bills (2 left stale); the workflow, re-enabled on 2026-09-23 after `6d34a1b`, was disabled again that day (decision 31), and plan A1 retired the writer: `bill-family` owns `congress_bills` alone, re-reads every `congress_api_list` row in its scope and merges `update_date` by the larger value (receipt `drift-audit-2026-09-23/`) |
| T11 | `run-rollup-unified-agenda` | `unified_agenda.parquet` | generated and verified for the retained edition; qualified at `8a22c704…` (2026-09-23), carried forward: table bytes equal `5ec1daa4…`'s and so the source-qualified `ea589343…`; the run re-read edition 202510, still reginfo's newest listed, and the generation moved only because the build environment did (SpicyDocs 0.26.5 → 0.30.0) (receipt `drift-audit-2026-09-23/`) |
| T11 | `run-rollup-federal-register` | `federal_register.parquet` | source-audited and qualified at `cf89be63…` (2026-09-23): 1,009,103 rows, 1994-01-03 to 2026-09-23; `731984ca…`'s rows (every month equal to the publisher's facet and 74 whole days matched in identity and every cell; details in the execution log) are unchanged but one publisher update (2026-19147's comment close date, now 2026-11-30), and the 98 added are the 2026-09-23 issue, equal to the publisher's daily facet and to a fresh read in every cell (receipt `drift-audit-2026-09-23/`) |
| T12 | `run-rollup-fcc-proceedings` | `fcc_proceedings.parquet` | published after a source replay validated against raw pages (`a1116f71…`); the scheduled whole walk carries the same table bytes and is qualified at `62715192…` (2026-09-23): 21,683 docket names from 21,691 ECFS documents, equal cell for cell to a fresh whole walk whose window counts equal its distinct documents; walked whole, one row per docket (details in the execution log) (receipt `t12-requalification-2026-09-23/`) |
| T12 | `run-rollup-fcc-filings` | `fcc_filings.parquet` | published after a source replay validated against raw pages (`c0c1aa4e…`, the bounded first run from 2026-08-24) and qualified at `87c99b2d…` (2026-09-23): 5,254 filings received 2026-08-24 to 09-23, the 5,137 carried cell for cell plus 117 appended; all 1,218 rows of the run's window (received from 09-15) equal a fresh ECFS walk whose count equals its distinct filings; incremental from here (receipt `t12-requalification-2026-09-23/`) |
| T14 | `run-rollup-sam-entities` | `sam_entities.parquet` | bounded initial load published (decision 10) and qualified at `56dd0f65…` (2026-09-23): all 147,254 active registrations dated 2026 from one retained bulk extract, cell-for-cell equal to it; the fork workflow was held until the fixes were pushed and re-enabled on 2026-09-23 (`SAM_API_KEY` is set); every scheduled run from then to 2026-09-23 failed on the reader's download `Accept` header (HTTP 406); SpicyDocs 0.31.0's reader, a 75-minute job and one wall-clock wait across the run's years are in `aaa1141` (pushed 2026-09-23), and the next scheduled run is the proof (see Operations) |
| T14 | `run-rollup-lobbying-filings` | `lobbying_filings.parquet` | bounded initial load published (decision 10), advanced by the first scheduled run after the workflow was re-enabled on 2026-09-23 (`LDA_API_KEY` is set), and qualified at `03d474bd…` (2026-09-23): all 27,896 filings posted 2026-07-01 through 2026-09-22; the run's window (posted 2026-09-14 to before 2026-09-23, the watermark minus the 7-day overlap) declares 317 filings, equal to its distinct filings and to the table's rows so posted, and equal to a fresh keyed read in every cell (33 added, none removed or changed); the other 27,579 rows are cell-equal to `a9fd5de6…` and to its retained raw pages, JSON columns included (receipt `lobbying-requalification-2026-09-23/`) |
| T04 | `run-rollup-fec-committees` | `fec_committees.parquet` | generated and verified; qualified at `dfda14da…` (2026-09-21) |
| T04 | `run-rollup-fec-source-catalog` | `fec_source_catalog.parquet` | generated and verified; qualified at `0570574b…` (2026-09-21) |
| T04 | `build-fec-observations` | `fec_source_records.parquet`, `fec_collections.parquet`, `fec_relationships.parquet` | generated and verified; qualified at `11bcb620…` (2026-09-21) |
| T15 | `run-rollup-org-committee-links` | `org_committee_links.parquet` | published 2026-09-23 as `7dd4e95a…` (1,114 links), rebuilt on the ACF parent and qualified at `e3d05e5b…` (2026-09-23): 1,186 name-matched links; every committee and comment count verified (the match itself is a stated heuristic with confidence tiers) |
| T12 | `run-rollup-gao-reports` | `gao_reports.parquet` | published by schedule and qualified at `57b38147…` (2026-09-23): 31 reports published 2026-09-15 to 09-23, the source-qualified `8050ae13…` carried forward cell for cell (25 rows) plus 6 appended; every cell equals the raw GAO feed item (captures of 2026-09-22 and 2026-09-23) and each `report_id` equals the feed's `product_id` and `guid`. `abstract` keeps the feed's double-escaped `&nbsp;` in 7 rows; the generation retains no source evidence (receipt `gao-requalification-2026-09-23/`) |
| T12 | `run-rollup-crs-reports` | `crs_reports.parquet` | published after a source replay validated against raw pages (`6c15aac2…`) and qualified at `a7d115f8…` (2026-09-23): 14,138 reports, the publisher's declared count; 14,117 carried cell for cell, and the 1 added and 20 updated match a fresh read in every cell but 16 `update_date` values the publisher re-stamped after the run; `R45210`'s title carries the publisher's dropped hyphen (details in the execution log) (receipt `t12-requalification-2026-09-23/`) |
| T12 | `run-rollup-courtlistener` | `court_dockets.parquet` | generated and verified: enriched APA selection published (11,459 rows, `cbb99244…`), extended by schedule and qualified at `95f2091d…` (2026-09-23): 11,470 rows, 11,456 carried cell for cell, 11 dockets filed 2026-09-17 to 09-22 added and 3 updated by the publisher; all 35 rows of the run's window (filed from 09-04) equal a fresh CourtListener search cell for cell (receipt `t12-requalification-2026-09-23/`) |
| T12 | `build_court_docket_groups` | `court_docket_groups.parquet` | republished with numeric parent order and qualified at `0f855eb1…` (2026-09-23): 901 rows; raw-validated against the native docket edition and public readback identical |
| T12 | `run-rollup-usaspending-recipients` | `usaspending_recipients.parquet` | published by schedule; qualified at `380f5b32…` (2026-09-23) with recorded source drift: 10,117 rows, `18e51cf5…`'s 10,000 plus 117 entrants; 9,006 of 9,854 recipients in a same-day capture equal it in every cell, 848 amounts moved after it, and fresh detail reads match every identity cell. `total_award_amount` is the publisher's trailing-12-month amount (the dictionary said all-time until `7731e9b`), and 117 rows keep earlier runs' amounts (details in the execution log) (receipt `t12-requalification-2026-09-23/`) |
| T09 | `run-rollup-bill-family` | `congress_bills.parquet`, `bill_actions.parquet`, `bill_committees.parquet`, `bill_publisher_summaries.parquet`, `bill_versions.parquet`, `bill_sections.parquet`, `section_diffs.parquet`, `section_diff_items.parquet`, `financial_changes.parquet`, `section_classifications.parquet`, `bill_summaries.parquet`, `diff_summaries.parquet`, `cbo_cost_estimates.parquet`, `public_activity_events.parquet`, `bill_family_archives.parquet`, `bill_vote_references.parquet`, `bill_family_backfills.parquet`, `bill_family_backfill_walks.parquet` | published (decision 1) and qualified at `a846cb44…` (2026-09-23), the reconciled generation: the accepted 419,839-bill candidate plus the then-live scheduled 119th rows; conservation and row provenance verified (details in the execution log). `bill_vote_references.date` holds the UTC instant, whose day differs from the chamber's vote day for 91 of the 847 vote–date pairs that join `roll_call_votes` (145 of 1,347 joined rows; survey A11). Scheduled generation `c789aaa9…` (the narrow writer's; see T16) is not qualified: 17 tables are byte-equal and conservation holds, but `congress_bills` replaced 3,044 `update_date` instants with same-day dates (against the documented larger-value merge) and 3,095 page URLs with API resource URLs, and skipped 13 in-window bills (receipt `drift-audit-2026-09-23/`) |
| T08 | `run-rollup-press-releases` | `press_releases.parquet` | reviewed bounded relationship correction (`f9bc0f4c…`) carried forward; qualified at `e011fbfa…` (2026-09-23): 30 rows; the possessive `2027` stays unmatched and the four literal H.R. links are unchanged; the 2 added and 23 other in-feed items match fresh RSS in every cell but capture times (receipt `t08-requalification-2026-09-23/`) |
| T08 | `run-rollup-amendments` | `amendments.parquet` | republished with detail and qualified at `c943cb37…` (2026-09-23), table `f8c3943a…`: 7,095 = the source's declared 119th count, one detail response per amendment; every cell matches the retained list and detail responses; 89 Rules Committee amendments have no member sponsor |
| T08 | `run-rollup-member-vote-terms` | `member_vote_terms.parquet` | added and published (decision 2); qualified at `890481eb…` (2026-09-23): 382,136 rows; exceptions equal the independent join replay row for row |
| T08 | `run-rollup-roll-call-votes` | `roll_call_votes.parquet` | native fields verified; derived bill links qualified at `3204b8fc…` (2026-09-23): all 845 links are recorded votes in their bill's raw GovInfo BILLSTATUS (509 bills) and in the published references, no vote is claimed by two bills, and none of the 730 unmatched votes is named by any reference; the two votes added since the qualified 1,573 (`119-senate-2-239`, `-240`) match the Senate's raw XML in every tally and all 100 positions |
| T08 | `run-rollup-roll-call-votes` | `member_votes.parquet` | generated and verified for the frozen 119th Congress selection plus two later Senate votes; qualified at `3204b8fc…` (2026-09-23): 382,136 rows; all 381,936 rows of `80028c18…` are unchanged, and the 200 added (`119-senate-2-239`, `-240`) equal freshly fetched Senate XML in every cell; both chambers' vote lists end at the table's last roll (receipt `derived-requalification-2026-09-23/`) |
| T08 | `run-rollup-members` | `members.parquet`, `member_terms.parquet` | generated and verified; fresh retained source observations; qualified at `017366cc…` (2026-09-23), carried forward: both equal the source-qualified `c4b48900…` except capture times |
| T10 | `run-rollup-committee-reports` | `committee_reports.parquet` | generated and verified; qualified at `98188de3…` (2026-09-23): 113 reports, byte-identical to `52f30e7c…`'s table; the 105 carried from `f5f16948…` differ from it only in capture time, each equal to the run's retained capture, and the 8 added match fresh GovInfo summaries, root MODS and bodies in every cell. `CRPT-119hrpt811` was refused, not published: its multi-part MODS also names `-pt1`, which SpicyDocs rejects (receipts `reports-requalification-2026-09-23/`, `hearing-clocks-2026-09-23/`) |
| T10 | `run-rollup-committee-reports` | `report_sections.parquet` | generated and verified; qualified at `98188de3…` (2026-09-23): 1,335 sections, byte-identical to `52f30e7c…`'s table; the 1,246 carried from `f5f16948…` are unchanged in every column, and the 89 added partition the 8 new reports' fresh GovInfo text exactly (receipts `reports-requalification-2026-09-23/`, `hearing-clocks-2026-09-23/`) |
| T10 | `run-rollup-committee-reports` | `hearing_transcripts.parquet` | published; native content verified; qualified at `98188de3…` (2026-09-23): 23 hearings; equal to `52f30e7c…` except the `observed_at` of the 6 hearings added by scheduled run 35684422112, replaced by a run that re-read only those six with every response retained: each body is byte-identical to its published digest and each new time equals a retained body capture. The 4 added earlier match fresh GovInfo in every cell (`CHRG-116shrg63674`'s `last_modified` has since moved at the publisher, body unchanged); the 13 older capture times match the retained acquisition ledger (receipts `reports-requalification-2026-09-23/`, `hearing-clocks-2026-09-23/`) |
| T10 | `run-rollup-committee-reports` | `hearing_bill_links.parquet` | generated and verified for selected root MODS COVER scope; qualified at `98188de3…` (2026-09-23): empty, byte-identical to `52f30e7c…`'s table; no root MODS of the 23 hearings names a COVER bill (receipts `reports-requalification-2026-09-23/`, `hearing-clocks-2026-09-23/`) |
| T10 | `run-rollup-committee-reports` | `committee_report_reads.parquet` | published; native content verified; qualified at `98188de3…` (2026-09-23): 137 checkpoints; 131 equal `52f30e7c…`'s (118 match that run's retained journal, 13 the local rebuild's log); the 6 of scheduled run 35684422112 were re-read and carry the re-read run's retained selection clock, with outcome, `last_modified` and rule version unchanged. The generation's metadata records SpicyDocs 0.30.0 as the build environment; the CRPT rows are still 0.26.6 reads, which each row's `rule_version` records. The CRPT rule version carries the SpicyDocs version, so the first scheduled run on 0.31.0 re-reads all 114 CRPT checkpoints (within the 200-per-run cap) before any new report (receipts `reports-requalification-2026-09-23/`, `hearing-clocks-2026-09-23/`) |
| T08 | `run-rollup-print-citations` | `house_activity_reports.parquet`, `budget_volumes.parquet`, `bill_committee_actions.parquet`, `document_citations.parquet` | generated and verified; qualified at `6b866755…` (2026-09-23): a full re-read under SpicyDocs 0.26.6 left every row (20, 16, 8,209 and 25,011) cell-identical to the replayed `ed76aea5…`. In `document_citations`, SpicyDocs' reader gives at least 301 of 2,741 `usc_section` rows (304 with unsplit hyphen ranges) and 4 of 41 `cfr_section` rows malformed keys, all marked resolved (trailing punctuation, dashes, ranges; survey A10) (receipt `t08-requalification-2026-09-23/`) |
| T08 | `run-rollup-senate-expenditures` | `senate_expenditures.parquet` | generated and verified; replayed byte-identical; qualified at `acef9fcf…` (2026-09-22) |
| T08 | `run-rollup-laws` | `laws.parquet`, `law_code_sections.parquet`, `table3_records.parquet` | qualified at `08a1c0bf…` (2026-09-23) by clean source replay: all 113 laws, 3,655 code-section rows and 65 Table 3 rows match in every native cell; only the laws' capture times differ |
| T08 | `run-rollup-committee-rosters` | `committees.parquet`, `committee_assignments.parquet` | qualified at `e35ff03d…` (2026-09-23) by clean source replay: all 236 committees and 2,966 assignments match in every native cell; only publisher activity counts grew since publication; decision 5: House select aliases live, 28 seats stay unlisted for want of a publisher link (documented) |
| T08 | `run-rollup-house-communications` | `house_communications.parquet` | generated and verified; qualified at `087a437f…` (2026-09-23): 4,992 rows; 28 added since `f35534f5…` and 979 rows' NULL detail filled at unchanged stamps; list fields match a fresh walk and 75 detail rows (21 added, 52 filled) match raw detail cell for cell. 2,992 rows are still list-only (1,000 detail reads per run). The 15 identities once called publisher-withdrawn were list-walk repeats, not withdrawals (receipt `t08-requalification-2026-09-23/`) |
| T08 | `run-rollup-committee-meetings` | `committee_meetings.parquet` | generated and verified; qualified at `4ff9bff5…` (2026-09-23): 2,758 rows; 5 added since `00f2fd89…`, 970 rows' NULL detail filled and 11 re-read after publisher updates; list fields match a fresh walk and 61 detail rows match raw detail cell for cell. 790 rows are still list-only; Senate meeting 338764 now answers 404 and two rows predate later publisher updates (receipt `t08-requalification-2026-09-23/`) |
| T08 | `run-rollup-record-issues` | `record_issues.parquet` | generated and verified; qualified at `5b57b8f0…` (2026-09-23): 365 rows; the 2 issues added since `045adce9…` match raw detail in all 17 cells and the other 363 are unchanged (receipt `t08-requalification-2026-09-23/`) |
| T08 | `run-rollup-treaties` | `treaties.parquet` | generated and verified; qualified at `491b6bd6…` (2026-09-23), carried forward: its table bytes equal the source-qualified `7007ca03…` (receipt `t08-requalification-2026-09-23/`) |
| T08 | `run-rollup-nominations` | `nominations.parquet` | generated and verified; qualified at `8fb0a72e…` (2026-09-23), carried forward: its table bytes equal the source-qualified `4a40b55e…`; its 2,204 identities equal a fresh list walk (receipt `t08-requalification-2026-09-23/`) |
| T06 | `run-pipeline` | `dockets.parquet` | generated and verified at table digest `27a2ed4a…` (2026-09-21; ETag `cae2e494…`), written by the local T06 delivery; no scheduled ETL has written it (see Operations) |
| T06 | `run-pipeline` | `documents.parquet` | generated and verified at table digest `7907ebe3…` (2026-09-22; ETag `6c63cb08…`), written by the local T06 delivery; no scheduled ETL has written it (see Operations) |
| T06 | `run-pipeline` | `comments_index.parquet` | published with the comments cohorts and verified at table digest `ba31f99f…` (2026-09-23; ETag `b0485f6f…`): 112,885 groups summing to all 23,890,403 comments |
| T06 | `run-pipeline` | `comments/agency_code=<agency>/docket_id=<docket>/year=<year>/month=<month>/part-0.parquet` | unproduced on the fork (probed keys answer 404); the comments deliveries wrote `comments.parquet` and its index directly |
| T07 | `publish-comments-mirror.yml` | `comments.parquet` | published (decision 7) and verified at table digest `c3f45f07…` (2026-09-23; ETag `cce5e386…`): 23,890,403 rows across all 133 comment-bearing agencies; six plus ACF re-verified and repaired from native source after independent review, the other ~126 carrying the retained parent's rows and awaiting the same repair. Derived attachment text repaired (plan A6, decision 19): 65,994 comments re-read through SpicyDocs' Mirrulations derived-text reader carry status `derived` with attachments joined in numeric order and one tool per comment recorded in `pdf_extraction_results_json`; 4,851 rows keep `ok` (4,816 re-extract byte-identical from their PDFs) and 768 `empty`; the other 23,818,790 rows are identical to the replaced object `fca7afb7…` (checked by an md5 of every column), no id lost or added, index unchanged; readback from S3 and the public URL equals the candidate (receipt `comment-text-repair-2026-09-23/`) |
| T07 | `publish-comments-mirror.yml` | `comments/agency/agency_code=<agency>/part-0.parquet` | blocked: the mirror refuses without `R2_CATALOG_*`, which the fork lacks, and every scheduled run fails on that check |
| T17 | `materialize-rulemaking` | `rule_targets.parquet`, `proceedings.parquet`, `regulatory_agenda_items.parquet`, `agenda_item_proceedings.parquet`, `comment_periods.parquet` | rebuilt on SpicyDocs 0.31.0 with label-aware docket reads, unpadded FR keys and the Eastern-day rule (plans A7, A12; decision 18) and published as `snapshot_21818212…` (2026-09-23; superseding `snapshot_0e799850…`), then re-run by schedule and qualified at `snapshot_837754c3…` (2026-09-24), the first scheduled snapshot: every cell of its five tables equals the audited candidate except the run stamps and the per-hop lineage columns (`supersedes_id` is each continuing row's own id, `identity_predecessors_json` is empty because no id changed against `snapshot_21818212…`; the 52,215 retirements are recorded in that snapshot's tables) (bootstrapped against the five audited parents; details in the execution log): 559,654 rule targets, 462,939 proceedings, 38,406 agenda items, 155,636 agenda–proceeding relationships and 286,770 comment periods, read from dockets `27a2ed4a…`, documents `7907ebe3…`, Federal Register `cf89be63…`, agenda `8a22c704…` and links `9bbeada2…`. Missing FR document references fall from 49,403 to 8,036 (46,945 resolve by unpadding, 1,216 by folding dashes or case, 34 stay ambiguous); labelled docket values join 154,943 link rows where 48,169 did; no evidence, docket or document is lost; 23,959 comment-period ids and 38,034 agenda-relationship ids change and 52,215 proceeding ids retire as predecessors; the Eastern-day rule moves 5,446 first-seen and 73,953 last-seen days one day earlier; all 25 integrity checks are zero, 30 of 30 FR API samples and 10 of 10 changed rows match the raw records. 5,807 unread link rows still name a held docket inside a label or list (887 lists): SpicyDocs 0.31.0's `normalize_docket_references` reads them and is the next adoption (receipts `rulemaking-joins-2026-09-23/candidate-0.31.0/`, `…/scheduled-837754c3/comparison.json`) |

## Open items

The dated narrative behind the rows (the September 21–22 status, the scope gaps and the September 23 continuation) is in the [fork execution log](fork-execution-log-2026-09-23.md); "plan" below is SpicyDocs' `docs/research/consolidation-path-2026-09-22.md`.

- **Newer scheduled generations** await audit (decision 3); `scripts/check_ledger_pins.py` lists the rows they affect. `house_communications` and `committee_meetings` will drift daily while their detail reads catch up (1,000 per run; about three more runs and one more run respectively).
- **CFR citations:** `cfr_ref` is NULL on the 7,910 range, parenthesized and typo rows by ruling 5; nothing further is planned (receipt `cfr-replace-all-2026-09-23/`).
- **Rulemaking joins:** published (plan A7, A12; ruling 6) and re-run by schedule at `snapshot_837754c3…`. Next: read labelled docket lists through SpicyDocs' `normalize_docket_references` (5,807 link rows; in review), and rule on non-rulemaking dockets forming proceedings and on the merge of dockets an omnibus document names together (blind ruling pending).
- **Comment and document text:** the derived-text repair is published (plan A6, ruling 7; the 13 attachments whose derived text Mirrulations does not hold are recorded in the receipt); document bodies, extraction status and PDF-extraction evidence are still null (log: Scope and evidence gaps).
- **Other parsing-survey limits:** `document_citations` keys (A10; the grammar move, plan B4, is in rework after review). Vote days (A11) are fixed in code (`57a68bc`, pushed): the next scheduled roll-call run backfills `roll_call_votes.vote_day`, after which `bill_vote_references` joins `vote_id` to it for the chamber's day. The `bill_subjects` key fix (A5) is pushed (log: Parsing survey).
- **Comments:** native repair of the agencies beyond six plus ACF; the dated partition tree is unproduced, and the mirror is blocked on `R2_CATALOG_*` (log: ACF comments cohort, Validation sweep).
- **Bill family:** missing bodies and original text, models and historical backfills (log: Scope and evidence gaps, Bill family).
- **Wider populations:** SAM and lobbying beyond their bounded loads; FCC filings before the first window; USAspending beyond its top-100-page selection; CourtListener's full scope, rate budgets, newer catch-up and the non-civil docket scope decision (log: Scope and evidence gaps, Latest continuation).
- **Lifecycles:** a pairing and unknown-docket policy before `rulemaking_lifecycles` returns (decision 4).
- **Multi-part reports:** `CRPT-119hrpt811` is refused because SpicyDocs rejects the `-pt1` identifier its MODS names. SpicyDocs' reader now publishes one row per part (plan B31, decision 29, owner confirmation of the identity move pending); `committee_reports` adopts it after the re-vendor and backfills `part_id` on its rows first.
- **Pooled enumeration:** pooled walks compare counts, not identity sets; plan B6, on SpicyDocs' 0.31.0 release branch, pools identities across up to four differently paged walks and settles on a clean walk or a pool equal to the declared count; spicy-regs adopts it at the re-vendor (log: CRS reports and FCC).
- **FEC (T05):** broader individual records, committee history and correction streams (log: Scope and evidence gaps).
- **Operations (T18–T20):** the workflows held for the push were re-enabled on 2026-09-23 (decision 22) and the `bill_subjects` key fix (`81cfee7`) was pushed the same day; the narrow `congress_bills` writer was disabled again that day (decision 31). The scheduled ETL has never completed on the fork: with no `manifest.parquet` each batch re-fetches its agencies' full history and is cancelled at 60 minutes (run 35768282116: batch 0 at AMS 250,000 of 349,359 and APHIS 175,000 of 552,195 when cancelled; batch 7 at FWS 125,000 of 2,637,380), and batches that finish staging fail on the absent `R2_CATALOG_*`, so nothing is published and the four base objects were all written by local deliveries; it needs a manifest seeded from the fork's published ids (a copy of upstream's would skip VA and USCIS, whose upstream manifest keys exceed its index) and a provisioned, seeded catalog, and its batch size must cover 335 discovered agencies (five are in no batch). `sam_entities` failed on every scheduled run to 2026-09-23: the reader's extract download sent `Accept: application/json` to an endpoint that serves only gzip (HTTP 406; run 35907412992; receipt `sam-406-2026-09-23/`); SpicyDocs 0.31.0 fixes the header and waits by wall clock, vendored in `aaa1141` with a 75-minute job. `bill_subjects` has never started by schedule: all 19 runs since 2026-09-05 failed at startup on an undeclared workflow input, declared in `e3dd373` (merged `b7283e4`, pushed 2026-09-23). `GEMINI_API_KEY` and `R2_CATALOG_*` are absent, Zyte is unwired, Pages is not enabled and dictionary deploys fail (log: Validation sweep).
