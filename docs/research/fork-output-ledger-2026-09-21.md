# Fork output ledger — September 21, 2026

This dated ledger accounts for every declared rollup output plus base regulatory, comments and rulemaking outputs at code `ea64719`. It records 44 producer paths and 82 distinct output keys, including dynamic partition patterns.

The [consolidated backlog](../fork-generation.md) remains the task list. [Machine-readable evidence](/Users/mikewolfd/Work/corpora/fork-execution-2026-09-21/output-ledger.json) records each candidate path, measured size/count/schema, source assessment, scope, required and optional inputs, observed publication pins, next action and receipt. A missing pin or unresolved scope remains a blocker. Existing populations must survive narrower repairs.

- **Pins.** A row records `qualified at <pin> (<date>)` for the generation its audit covered, not a claim that the pin is live; `scripts/check_ledger_pins.py` reads the public index and reports rows whose live generation differs. Withdrawn, unproduced, blocked and known-wrong outputs carry no such phrase, and base objects outside the index (dockets, documents, docket search, comments and its index) record `verified at table digest <digest> (<date>; ETag <etag>)`, whose ETag the script compares with a HEAD of the public object (a row without the ETag is reported unpinned).
- **Names.** A generation is named by the first 8 hex digits of its `artifactDigest`; a table digest is labelled "table"; units are named (rows, distinct keys, pairs).
- **Evidence.** Every load-bearing number cites a retained script or receipt; numbers copied from a publisher's statement cite the receipt.

Public data destination: `https://pub-72e95c0c20a84508b42b03a6ff6d55f8.r2.dev`. Local candidates are not fork publications. Retained qualified generations include FEC, members/terms, nominations, treaties, the five-member report family, repaired dockets/documents, docket search, two document-derived tables, Appropriations press-feed windows, the retained Agenda edition and complete June 30 court clusters. As of 2026-09-23 the following are also source-qualified or validated against raw data:
- **Regulatory:** the Federal Register and its docket links; the rulemaking dataset; the comments table (all 133 comment-bearing agencies, with six plus ACF re-verified and repaired from native source) with its index and the three T15 summaries.
- **Legislative:** laws; committee rosters; amendments with their sponsor and amended-bill detail; the reconciled bill family (decision 1) and its subjects; member vote terms (decision 2); roll-call bill links.
- **Courts and other sources:** the court citation tables and opinion index; CRS reports; FCC proceedings and filings; bounded SAM and lobbying loads (decision 10).

CFR's parts are placed from each volume's `PART` heading since generation `de703ffe…` (see the CFR part ancestry entry in the [execution log](fork-execution-log-2026-09-23.md)). The September 23 parsing survey found join, text and key limits in some qualified tables; each affected row names them. Committee meetings, house communications, record issues, print citations, Senate expenditures and GAO reports were published by scheduled runs and source-qualified at the generations their rows name; the qualification receipts are in `scheduled-published-qualification/` under the execution receipts. Scheduled generations newer than a row's qualified pin await audit (decision 3). Both repositories were pushed on 2026-09-23 and the workflows held for the push were re-enabled that day (decisions record, decisions 11 and 22).

| Task | Producer | Output | Delivery state |
| --- | --- | --- | --- |
| T13 | `run-rollup-court-opinion-clusters` | `court_opinion_clusters.parquet` | generated and verified for complete 2026-06-30 edition; qualified at `7a2cbdb7…` (2026-09-22) |
| T13 | `run-rollup-court-opinion-bodies` | `court_opinion_bodies.parquet` | withdrawn 2026-09-22 and removed 2026-09-23: builder, rollup, workflow and catalog/MCP registration deleted, and the push deleted the fork workflow; text links out through the clusters' `absolute_url` |
| T13 | `run-rollup-court-citations` | `court_citations.parquet`, `court_citation_map.parquet`, `court_parentheticals.parquet` | added and published for the complete 2026-06-30 edition: qualified at `f1e2e523…` (2026-09-22); public row counts verified |
| T13 | `run-rollup-court-opinions` | `court_opinions.parquet` | added and published for the complete 2026-06-30 edition (text-free): qualified at `f7cc67cc…` (2026-09-22); public row counts verified; no scheduled workflow (54.6 GB source) |
| T16 | `run-rollup-bill-subjects` | `bill_subjects.parquet` | published and qualified at `86137cc2…` (2026-09-23): the retained 20,013-bill enrichment (GovInfo BILLSTATUS, enriched 2026-08-22); every bill exists in the reconciled family, its 5,965 BILLSTATUS-bearing bills agree on every policy area and all but one subject set, and 25 random others match raw BILLSTATUS exactly; no scheduled run has started (see Operations). From `b7283e4` (plan P6) the rollup copies BILLSTATUS subjects from the bill family for the 108th Congress on, reads the bulk zips of folders the family has not read, and asks the Congress.gov API only for older Congresses and refused files, under a run deadline and start-to-start pacing |
| T15 | `run-rollup-feed-summary` | `feed_summary.parquet` | published 2026-09-23 as `1a833d52…`, rebuilt on the ACF parent as `4c2e2cd9…` (every comment count and document date a direct recount from the parents) and qualified at `0caab1d2…` (2026-09-23): 279,124 dockets, cell for cell equal to `4c2e2cd9…` on unchanged parents (only row order differs) (receipt `drift-audit-2026-09-23/`) |
| T15 | `run-rollup-agency-stats` | `agency_stats.parquet` | published 2026-09-23 as `d4fa809a…`, rebuilt on the ACF parent as `a9b3d6fc…` (every count a direct recount) and qualified at `b3d816c0…` (2026-09-23): 316 agencies; table bytes equal `a9b3d6fc…`'s (receipt `drift-audit-2026-09-23/`) |
| T15 | `run-rollup-agency-monthly-volume` | `agency_monthly_volume.parquet` | generated and verified; qualified at `5106061c…` (2026-09-23): 77,934 rows equal row for row to `bb0d6255…` (only row order differs; footer input `7907ebe3…`), which equalled two separate re-derivations from `documents.parquet` table `7907ebe3…` (1,999,503 dated rows; 2,020 null and 8 year-zero dates omitted) (receipts `derived-requalification-2026-09-23/`, `drift-audit-2026-09-23/`) |
| T15 | `run-rollup-docket-search` | `docket_search.json.gz` | verified at table digest `9f383461…` (2026-09-24; ETag `1bc3c9a8…`): all 279,106 documents equal the qualified `230e55ae…` payload by identity and every field, with no duplicate ids; only the build timestamp and gzip bytes changed. Parent dockets remains `27a2ed4a…` (receipt `ledger-continuation-2026-09-24/docket-search/`) |
| T15 | `run-rollup-lifecycles` | `rulemaking_lifecycles.parquet` | withdrawn 2026-09-23 (decision 4): the 2026-09-22 scheduled run had published generation `2f001194…` (26,519 rows); the family was conditionally removed from the index. Its workflow was deleted in `5715163` on September 24; the local research producer remains and pairing semantics remain blocked |
| T15 | `run-rollup-discovery-signals` | `discovery_signals.parquet` | generated and verified; qualified at `e17fe02f…` (2026-09-23), the UTC rule of `31dadbe5…` carried forward: its 6 rows equal a separate re-derivation from `documents.parquet` table `7907ebe3…` at its recorded `as_of` (2026-09-23 23:23:04 UTC), as `e3f835b8…`'s 9 rows did at theirs; the change is the moving window. No parent row lies between the UTC and New York readings of any window boundary, so the data does not exercise the rule; it rests on the recorded date policy (receipt `derived-requalification-2026-09-23/ds-rederive-e17fe02f.json`) |
| T15 | `run-rollup-fr-docket-links` | `fr_docket_links.parquet` | rebuilt by schedule on `cf89be63…` and qualified at `9bbeada2…` (2026-09-23): its 899,377 rows equal a separate re-derivation from that parent row for row in both directions; against `92f99b00…` (itself equal to a re-derivation from `731984ca…`) it adds 150 rows for 76 of the 98 documents of the 2026-09-23 issue and updates one (2026-19147) (receipt `rulemaking-joins-2026-09-23/candidate-0.31.0/links-qualification.json`) |
| T11 | `run-rollup-cfr-sections` | `cfr_sections.parquet` | parts placed from each volume's `PART` heading since `de703ffe…` (2026-09-23): 319,501 rows, `part` changed on exactly 9,250 rows against `8fb97150…` (title 41 4,732, title 43 3,018, title 14 vol 4 1,492, 8 typo and range rows), `section` on 13,955 and `cfr_ref` on 10,886, every other column byte-equal, eCFR's 2025-01-01 structure agreeing on every compared row of titles 41, 43 and 14 and GovInfo MODS on 4,517 of 4,518 changed rows (receipt `cfr-ancestry-fix-2026-09-23/`). Republished by schedule as `f6192c07…` (2026-09-23): 321,010 rows; the new `CFR-2026-title9-vol1` adds 1,509 granules (GovInfo's declared count), each placed under its volume's heading; the publisher re-stamped `CFR-2025-title9-vol1`, whose 1,510 rows change only `last_modified`; the other 317,991 rows are identical to `de703ffe…`; 10 added rows match GovInfo (receipt `drift-audit-2026-09-23/`). A `replace_all` run on SpicyDocs 0.31.0 (run 35940291235) published `08e585dd…`, qualified at `08e585dd…` (2026-09-23): 321,010 rows, every cell equal to `f6192c07…` except `cfr_ref` on 2 rows (37 CFR 2.188, 7 CFR 760.2210) that regain their citation now a single section printed with `§§` reads as one; the candidate scan's earlier count of 12 was a range-flag count, not a count of missing citations. `cfr_ref` is the printed citation (ruling 5) and is NULL on 7,910 range, parenthesized and typo rows; title 41's compound-part citations agree with the citation grammar's key (receipt `cfr-replace-all-2026-09-23/`) |
| T16 | `run-rollup-congress-bills` (retired, A1) | `congress_bills.parquet` | the narrow writer's table is the reconciled family's `congress_bills` (qualified at `a846cb44…` (2026-09-23); see T09). Its run 35915872100 published `c789aaa9…`, not qualified: it replaced 3,044 `update_date` instants with same-day dates and 3,095 congress.gov page URLs with API resource URLs, and its list walk skipped 13 in-window bills (2 left stale); the workflow, re-enabled on 2026-09-23 after `6d34a1b`, was disabled again that day (decision 31), and plan A1 retired the writer: `bill-family` owns `congress_bills` alone, re-reads every `congress_api_list` row in its scope and merges `update_date` by the larger value (receipt `drift-audit-2026-09-23/`) |
| T11 | `run-rollup-unified-agenda` | `unified_agenda.parquet` | generated and verified for the retained edition; qualified at `8a22c704…` (2026-09-23), carried forward: table bytes equal `5ec1daa4…`'s and so the source-qualified `ea589343…`; the run re-read edition 202510, still reginfo's newest listed, and the generation moved only because the build environment did (SpicyDocs 0.26.5 → 0.30.0) (receipt `drift-audit-2026-09-23/`) |
| T11 | `run-rollup-federal-register` | `federal_register.parquet` | source-audited and qualified at `cf89be63…` (2026-09-23): 1,009,103 rows, 1994-01-03 to 2026-09-23; `731984ca…`'s rows (every month equal to the publisher's facet and 74 whole days matched in identity and every cell; details in the execution log) are unchanged but one publisher update (2026-19147's comment close date, now 2026-11-30), and the 98 added are the 2026-09-23 issue, equal to the publisher's daily facet and to a fresh read in every cell (receipt `drift-audit-2026-09-23/`) |
| T12 | `run-rollup-fcc-proceedings` | `fcc_proceedings.parquet` | published after a source replay validated against raw pages (`a1116f71…`); the scheduled whole walk carries the same table bytes and is qualified at `62715192…` (2026-09-23): 21,683 docket names from 21,691 ECFS documents, equal cell for cell to a fresh whole walk whose window counts equal its distinct documents; walked whole, one row per docket (details in the execution log) (receipt `t12-requalification-2026-09-23/`) |
| T12 | `run-rollup-fcc-filings` | `fcc_filings.parquet` | published after a source replay validated against raw pages (`c0c1aa4e…`, the bounded first run from 2026-08-24) and qualified at `87c99b2d…` (2026-09-23): 5,254 filings received 2026-08-24 to 09-23, the 5,137 carried cell for cell plus 117 appended; all 1,218 rows of the run's window (received from 09-15) equal a fresh ECFS walk whose count equals its distinct filings; incremental from here (receipt `t12-requalification-2026-09-23/`) |
| T14 | `run-rollup-sam-entities` | `sam_entities.parquet` | bounded initial load published (decision 10) and qualified at `56dd0f65…` (2026-09-23): all 147,254 active registrations dated 2026 from one retained bulk extract, cell-for-cell equal to it; the fork workflow was held until the fixes were pushed and re-enabled on 2026-09-23 (`SAM_API_KEY` is set); every scheduled run from then to 2026-09-23 failed on the reader's download `Accept` header (HTTP 406); SpicyDocs 0.31.0's reader, a 75-minute job and one wall-clock wait across the run's years are in `aaa1141` (pushed 2026-09-23), and the next scheduled run is the proof (see Operations) |
| T14 | `run-rollup-lobbying-filings` | `lobbying_filings.parquet` | bounded initial load published (decision 10), advanced by the first scheduled run after the workflow was re-enabled on 2026-09-23 (`LDA_API_KEY` is set), and qualified at `03d474bd…` (2026-09-23): all 27,896 filings posted 2026-07-01 through 2026-09-22; the run's window (posted 2026-09-14 to before 2026-09-23, the watermark minus the 7-day overlap) declares 317 filings, equal to its distinct filings and to the table's rows so posted, and equal to a fresh keyed read in every cell (33 added, none removed or changed); the other 27,579 rows are cell-equal to `a9fd5de6…` and to its retained raw pages, JSON columns included (receipt `lobbying-requalification-2026-09-23/`) |
| T04 | `run-rollup-fec-committees` | `fec_committees.parquet` | generated and verified; qualified at `dfda14da…` (2026-09-21) |
| T04 | `run-rollup-fec-source-catalog` | `fec_source_catalog.parquet` | generated and verified; qualified at `0570574b…` (2026-09-21) |
| T04 | `build-fec-observations` | `fec_source_records.parquet`, `fec_collections.parquet`, `fec_relationships.parquet` | generated and verified; qualified at `11bcb620…` (2026-09-21) |
| T15 | `run-rollup-org-committee-links` | `org_committee_links.parquet` | published 2026-09-23 as `7dd4e95a…` (1,114 links), rebuilt on the ACF parent and qualified at `e3d05e5b…` (2026-09-23): 1,186 name-matched links; every committee and comment count verified (the match itself is a stated heuristic with confidence tiers) |
| T12 | `run-rollup-gao-reports` | `gao_reports.parquet` | published by schedule and qualified at `57b38147…` (2026-09-23): 31 reports published 2026-09-15 to 09-23, the source-qualified `8050ae13…` carried forward cell for cell (25 rows) plus 6 appended; every cell equals the raw GAO feed item (captures of 2026-09-22 and 2026-09-23) and each `report_id` equals the feed's `product_id` and `guid`. `abstract` keeps the feed's double-escaped `&nbsp;` in 7 rows; the generation retains no source evidence (receipt `gao-requalification-2026-09-23/`) |
| T12 | `run-rollup-crs-reports` | `crs_reports.parquet` | published after a source replay validated against raw pages (`6c15aac2…`) and qualified at `a7d115f8…` (2026-09-23): 14,138 reports, the publisher's declared count; 14,117 carried cell for cell, and the 1 added and 20 updated match a fresh read in every cell but 16 `update_date` values the publisher re-stamped after the run; `R45210`'s title carries the publisher's dropped hyphen (details in the execution log) (receipt `t12-requalification-2026-09-23/`) |
| T12 | `run-rollup-courtlistener` | `court_dockets.parquet` | generated and verified: enriched APA selection published (11,459 rows, `cbb99244…`), extended by schedule and qualified at `95f2091d…` (2026-09-23): 11,470 rows, 11,456 carried cell for cell, 11 dockets filed 2026-09-17 to 09-22 added and 3 updated by the publisher; all 35 rows of the run's window (filed from 09-04) equal a fresh CourtListener search cell for cell (receipt `t12-requalification-2026-09-23/`) |
| T12 | `build_court_docket_groups` | `court_docket_groups.parquet` | republished with numeric parent order and qualified at `0f855eb1…` (2026-09-23): 901 rows; raw-validated against the native docket edition and public readback identical |
| T12 | `run-rollup-usaspending-recipients` | `usaspending_recipients.parquet` | published by schedule; qualified at `380f5b32…` (2026-09-23) with recorded source drift: 10,117 rows, `18e51cf5…`'s 10,000 plus 117 entrants; 9,006 of 9,854 recipients in a same-day capture equal it in every cell, 848 amounts moved after it, and fresh detail reads match every identity cell. `total_award_amount` is the publisher's trailing-12-month amount (the dictionary said all-time until `7731e9b`), and 117 rows keep earlier runs' amounts (details in the execution log) (receipt `t12-requalification-2026-09-23/`) |
| T09 | `run-rollup-bill-family` | `congress_bills.parquet`, `bill_actions.parquet`, `bill_committees.parquet`, `bill_publisher_summaries.parquet`, `bill_versions.parquet`, `bill_sections.parquet`, `section_diffs.parquet`, `section_diff_items.parquet`, `financial_changes.parquet`, `section_classifications.parquet`, `bill_summaries.parquet`, `diff_summaries.parquet`, `cbo_cost_estimates.parquet`, `public_activity_events.parquet`, `bill_family_archives.parquet`, `bill_vote_references.parquet`, `bill_family_backfills.parquet`, `bill_family_backfill_walks.parquet` | published (decision 1) and qualified at `a846cb44…` (2026-09-23), the reconciled generation: the accepted 419,839-bill candidate plus the then-live scheduled 119th rows; conservation and row provenance verified (details in the execution log). `bill_vote_references.date` holds the UTC instant, whose day differs from the chamber's vote day for 91 of the 847 vote–date pairs that join `roll_call_votes` (145 of 1,347 joined rows; survey A11). Scheduled generation `c789aaa9…` (the narrow writer's; see T16) is not qualified: 17 tables are byte-equal and conservation holds, but `congress_bills` replaced 3,044 `update_date` instants with same-day dates (against the documented larger-value merge) and 3,095 page URLs with API resource URLs, and skipped 13 in-window bills (receipt `drift-audit-2026-09-23/`). Scheduled run 35946820267 (2026-09-24, `e70d7d8`) published nothing: its 17 other tables kept every row of `c789aaa9…` and only added rows (2,958 of the damaged dates and 2,959 of the URLs put back), but since `3192aac` a folder keeps its `bill_family_archives` row only after its bodies and XML pairs complete, which none can under the 600-fetch cap, so the shrink guard refused to drop the 8 pre-`3192aac` rows `a846cb44…` had carried over; `c789aaa9…` stays live; the rule is fixed in `24eac7d` (every visited folder keeps its row; completion stays in the metadata list; the replayed run passes the guard at 1.03), pushed 2026-09-24, and the next scheduled run is the proof |
| T08 | `run-rollup-press-releases` | `press_releases.parquet` | reviewed bounded relationship correction (`f9bc0f4c…`) carried forward through `e011fbfa…` and qualified at `3adc4f67…` (2026-09-24): 31 rows; the added Senate item (naming no bill) and the 24 others still in the feeds match fresh RSS in every cell but the Senate channel's rebuilt `lastBuildDate`; the 30 carried rows differ only in capture time, feed position and that stamp; the four literal H.R. links and the unmatched possessive `2027` are unchanged (receipt `drift-audit-2026-09-24/`) |
| T08 | `run-rollup-amendments` | `amendments.parquet` | republished with detail as `c943cb37…` (2026-09-23; every cell matching the retained list and detail responses; 89 Rules Committee amendments have no member sponsor) and qualified at `af5ef4c3…` (2026-09-24), table `3aa72d7e…`: 7,095 = the 119th's declared count; the pooled reader's one clean walk read the window's declared 37; 8 rows (Senate 6776–6783) carry the publisher's later latest action and match fresh list and detail responses, the other 29 re-read rows unchanged (receipt `drift-audit-2026-09-24/`) |
| T08 | `run-rollup-member-vote-terms` | `member_vote_terms.parquet` | added and published (decision 2); qualified at `8f055263…` (2026-09-24): 382,236 rows over all 1,576 roll calls from votes `054ec83f…` and members `5490e513…`; `890481eb…`'s rows unchanged; the 100 added are `119-senate-2-241`'s positions with `vote_day` 2026-09-23 as the Senate printed it; every cell equals the independent join replay, whose 18 exceptions are the table's 15 inclusive-end and 3 unmatched rows (receipt `drift-audit-2026-09-24/member-vote-terms/`) |
| T08 | `run-rollup-roll-call-votes` | `roll_call_votes.parquet`, `member_votes.parquet` | native fields verified and derived bill links qualified through `3204b8fc…` (all 845 links recorded votes in their bill's raw BILLSTATUS, none claimed twice); qualified at `054ec83f…` (2026-09-24), the first run since `57a68bc` (plan A11): 1,576 votes and 382,236 positions; `vote_day` is backfilled on every prior row and equals the chamber's printed date on all 20 checked against fresh chamber XML; the added `119-senate-2-241` matches its XML in every tally and all 100 positions; the UTC day of `bill_vote_references.date` runs a day ahead on 91 of 847 joined pairs, as surveyed; `119-senate-2-212`/`-213` keep an older `match_action_index` (21/19 where the first-action rule now gives 17/15), both real recorded-vote actions (receipt `drift-audit-2026-09-24/`) |
| T08 | `run-rollup-members` | `members.parquet`, `member_terms.parquet` | generated and verified; fresh retained source observations; qualified at `5490e513…` (2026-09-24), the first run on SpicyDocs 0.31.0: 12,770 members (539 current, 12,231 historical) and 45,535 terms; the run read the same crosswalk bytes as `c4b48900…` and `017366cc…` (current `19b49014…`, historical `25187633…`, unchanged upstream since 2026-09-03), so only capture times changed: every other cell equals the run's retained source and both prior generations, no member or term added or lost; Congress.gov's 539 current members are exactly the crosswalk's current roster, and the crosswalk's one known gap (Ed Case's 107th special-election term) is the source's (receipt `drift-audit-2026-09-24/members/`) |
| T10 | `run-rollup-committee-reports` | `committee_reports.parquet` | generated and verified; qualified at `ef08249e…` (2026-09-24), the first run on SpicyDocs 0.31.0: 119 reports; the 113 carried from `98188de3…` differ only in capture time and each equals the run's retained body capture; the 6 added match fresh GovInfo summaries, root MODS and bodies in every cell. Defect: `CRPT-119hrpt811` is among them, keyed on the package but holding Part 1's file, because 0.31.0 admits a MODS naming only `-pt1` and spicy-regs did not hold it (decision 29); every cell equaled GovInfo at that audit; the adopted multipart reader (B31, decision 29) is now published in `52325038…`, with identity `(package_id, part_id)` and `parts=per-part-001` checkpoints. Its source audit remains pending; see [Report refresh](#september-24-report-refresh). Earlier audit and adoption receipts remain in `drift-audit-2026-09-24/reports/` and `b31-adoption-2026-09-24/` |
| T10 | `run-rollup-committee-reports` | `report_sections.parquet` | generated and verified; qualified at `ef08249e…` (2026-09-24): 1,478 sections; the run rewrote every section and the unchanged reader (`report-headings-001`) reproduced the 1,335 carried exactly; the 143 added partition the 6 new reports' fresh GovInfo text exactly (51 of them `CRPT-119hrpt811`'s Part 1; see the reports row) (receipt `drift-audit-2026-09-24/reports/`); current publication `52325038…` awaits source audit (see [Report refresh](#september-24-report-refresh)) |
| T10 | `run-rollup-committee-reports` | `hearing_transcripts.parquet` | published; native content verified; qualified at `ef08249e…` (2026-09-24): 27 hearings; 22 byte-equal to `98188de3…`; `CHRG-116shrg63674` re-read because GovInfo re-stamped it (body byte-identical); the 4 added match fresh GovInfo in every cell and their `event_id`s match Congress.gov (one meeting, one absent, two 404s) (receipt `drift-audit-2026-09-24/reports/`); current publication `52325038…` awaits source audit (see [Report refresh](#september-24-report-refresh)) |
| T10 | `run-rollup-committee-reports` | `hearing_bill_links.parquet` | generated and verified for selected root MODS COVER scope; qualified at `ef08249e…` (2026-09-24): empty, byte-identical to `98188de3…`'s table; none of the 27 hearings' root MODS names a COVER bill (receipt `drift-audit-2026-09-24/reports/`); current publication `52325038…` awaits source audit (see [Report refresh](#september-24-report-refresh)) |
| T10 | `run-rollup-committee-reports` | `committee_report_reads.parquet` | published; native content verified; qualified at `ef08249e…` (2026-09-24): 146 checkpoints; the run re-read all 114 CRPT checkpoints (their `rule_version` now records SpicyDocs 0.31.0, `last_modified` unchanged) and 5 new reports under the 200-per-collection cap with nothing deferred, and fresh listings over its window equal the run's own; outcomes unchanged but `CRPT-119hrpt811` (refused → complete, the defect above) and `CHRG-116shrg63674` (detail_refused → complete); two new Senate hearings stay `detail_refused` (Congress.gov 404) and are retried each run (receipt `drift-audit-2026-09-24/reports/`); current publication `52325038…` awaits source audit (see [Report refresh](#september-24-report-refresh)) |
| T08 | `run-rollup-print-citations` | `house_activity_reports.parquet`, `budget_volumes.parquet`, `bill_committee_actions.parquet`, `document_citations.parquet` | published and qualified at `a5d46aa6…` (2026-09-24), reprocessing every held parent on SpicyDocs 0.32.1: 20 reports, 16 budget volumes, 8,209 actions and 26,379 citations. Against `2224d6a4…`, all eight lost citations return (seven Public Law schedule lines and `9 CFR 355`); four false GSA-prospectus RINs and four damaged-section prefix links disappear, and eight duplicate subpart links collapse. Every citation change matches the retained-text correction replay; the additional false RIN in CRPT-118hrpt974 is checked with a second PDF extractor. All 15,749 citation spans across six checked texts match their source slices; native parent cells and action cells are unchanged except rule versions, and changed parent counts equal direct recounts. All 36 parents have matching new successful-read checkpoints across their outputs. The ordinary 40-package cap leaves 41 discovery candidates deferred; the held-parent repair is complete. Generation, source evidence and direct public readback verified (receipt `ledger-continuation-2026-09-24/print-citations/`; grammar receipt `supply-2026-09-02/receipts/a10-grammar-followup-2026-09-24/`) |
| T08 | `run-rollup-senate-expenditures` | `senate_expenditures.parquet` | generated and verified; replayed byte-identical; qualified at `acef9fcf…` (2026-09-22) |
| T08 | `run-rollup-laws` | `laws.parquet`, `law_code_sections.parquet`, `table3_records.parquet` | qualified at `a86fe6cc…` (2026-09-24), the first run on SpicyDocs 0.31.0: 113 laws, 3,655 code-section rows and 65 Table 3 rows; every cell equals `08a1c0bf…` (itself a clean source replay) except the appended `usc_section_key`, which matches the decision-30 fold on all 3,655 re-read code-section rows and is NULL on the 65 Table III rows read once; laws 119-104, -108, -109 and -110 gained their GovInfo USLM and match it. The Table III walk is stuck at 65 rows: already-read acts are skipped when consecutive refusals are counted and OLRC's absent-act page reads as a transport failure, so 119-2, -3 and -5 stopped every run before the 38 later acts OLRC serves (true since `08a1c0bf…`). The walk now follows the chain each served page names under a 20-minute deadline (merged 2026-09-24, pushed; through SpicyDocs 0.32.0's `iter_table3_chain` since the re-vendor): 39 requests once, then one per Congress per run; a live 6-request check read 119-12 → -23 (643 rows), and the next scheduled run is the proof (receipts `drift-audit-2026-09-24/`, `table3-walk-2026-09-24/chain/`) |
| T08 | `run-rollup-committee-rosters` | `committees.parquet`, `committee_assignments.parquet` | qualified at `0c1bd785…` (2026-09-24) by clean source replay through `e35ff03d…`: 236 committees (byte-identical; the publisher declares 238 and serves 236) and 2,966 assignments, all 450 Senate seats matching the Senate file; only capture times and the Senate file date changed; decision 5: House select aliases live, the 28 seats unlisted for want of a publisher link are identical (receipt `drift-audit-2026-09-24/`) |
| T08 | `run-rollup-house-communications` | `house_communications.parquet` | generated and verified; qualified at `333e422d…` (2026-09-24): 4,992 rows, none added or removed; 1,000 list-only rows filled at unchanged stamps, newest first, no list column moved; 20 of them match raw detail in all 640 cells; 1,992 rows are still list-only (1,000 detail reads per run). The 15 identities once called publisher-withdrawn were list-walk repeats (receipts `t08-requalification-2026-09-23/`, `drift-audit-2026-09-24/house-communications/`) |
| T08 | `run-rollup-committee-meetings` | `committee_meetings.parquet` | published and qualified at `7ad94916…` (2026-09-24): all 2,762 prior rows retained; SpicyDocs 0.32.1 fills the 28 `NoChamber` meetings at unchanged source stamps, preserving `nochamber` in their identities. Those details and one ordinary publisher update match the retained fresh list and detail responses in all 783 cells; the other 2,733 rows are unchanged. No list-only rows remain. Senate 338764 remains carried although absent from the fresh list, whose 2,761 identities equal its declared count. Generation, source evidence and direct public readback verified (receipt `ledger-continuation-2026-09-24/committee-meetings/`; independent comparison `candidate-verification.json`) |
| T08 | `run-rollup-record-issues` | `record_issues.parquet` | generated and verified; qualified at `c3b1ce8f…` (2026-09-24): 366 rows; the added 172/150 matches raw list and detail in all 17 cells, the other 365 are unchanged, and volume 172's 147 listed issues equal the table's (receipts `t08-requalification-2026-09-23/`, `drift-audit-2026-09-24/record-issues/`) |
| T08 | `run-rollup-treaties` | `treaties.parquet` | generated and verified; qualified at `ec6f2d96…` (2026-09-24), carried forward: 2 rows, as `491b6bd6…` had, both held and not re-read; the table bytes equal the source-qualified `7007ca03…`; the fresh list's 2 treaties match every list cell (receipt `drift-audit-2026-09-24/treaties/`) |
| T08 | `run-rollup-nominations` | `nominations.parquet` | published and qualified at `f4d1a9a3…` (2026-09-24): 2,214 rows, preserving all 2,204 prior identities. The source-owned pooled reader settles on the fresh 237-record-page walk: 2,212 distinct nominations equal the publisher declaration, and all 28,756 list cells match the candidate. Ten additions include the four previously missed nominations (PN1022-7, PN1022-31, PN730-31, PN753) and six newly listed parts; 204 prior rows update and 2,000 are unchanged. PN1189 and PN1266 survive as prior parent identities although the publisher now lists their parts. Generation, source evidence and direct public readback verified (receipt `ledger-continuation-2026-09-24/nominations/`; independent comparison `candidate-verification.json`) |
| T06 | `run-pipeline` | `dockets.parquet` | generated and verified at table digest `27a2ed4a…` (2026-09-21; ETag `cae2e494…`), the qualified local T06 delivery. Hosted batch 0 has since published a newer object; its source audit is pending (see Operations) |
| T06 | `run-pipeline` | `documents.parquet` | generated and verified at table digest `7907ebe3…` (2026-09-22; ETag `6c63cb08…`), the qualified local T06 delivery. Hosted batch 0 has since published a newer object; its source audit is pending (see Operations) |
| T06 | `run-pipeline` | `comments_index.parquet` | published with the comments cohorts and verified at table digest `ba31f99f…` (2026-09-23; ETag `b0485f6f…`): 112,885 groups summing to all 23,890,403 comments at that pin. Hosted batch 0 has since advanced the public index; the monolith awaits the post-catch-up mirror refresh (see Operations) |
| T06 | `run-pipeline` | `comments/agency_code=<agency>/docket_id=<docket>/year=<year>/month=<month>/part-0.parquet` | unproduced on the fork (probed keys answer 404); the comments deliveries wrote `comments.parquet` and its index directly |
| T07 | `publish-comments-mirror.yml` | `comments.parquet` | published (decision 7) and verified at table digest `c3f45f07…` (2026-09-23; ETag `cce5e386…`): 23,890,403 rows across all 133 comment-bearing agencies; six plus ACF re-verified and repaired from native source after independent review, the other ~126 carrying the retained parent's rows and awaiting the same repair. Derived attachment text repaired (plan A6, decision 19): 65,994 comments re-read through SpicyDocs' Mirrulations derived-text reader carry status `derived` with attachments joined in numeric order and one tool per comment recorded in `pdf_extraction_results_json`; 4,851 rows keep `ok` (4,816 re-extract byte-identical from their PDFs) and 768 `empty`; the other 23,818,790 rows are identical to the replaced object `fca7afb7…` (checked by an md5 of every column), no id lost or added, index unchanged; readback from S3 and the public URL equals the candidate (receipt `comment-text-repair-2026-09-23/`) |
| T07 | `publish-comments-mirror.yml` | `comments/agency/agency_code=<agency>/part-0.parquet` | publication pending after local catch-up. Catalog credentials and the full seed are now configured; the shared completion chain will build this agency tree and verify it against the monolith and index |
| T17 | `materialize-rulemaking` | `rule_targets.parquet`, `proceedings.parquet`, `regulatory_agenda_items.parquet`, `agenda_item_proceedings.parquet`, `comment_periods.parquet` | rebuilt on SpicyDocs 0.31.0 with label-aware docket reads, unpadded FR keys and the Eastern-day rule (plans A7, A12; decision 18) and published as `snapshot_21818212…` (2026-09-23; superseding `snapshot_0e799850…`), then re-run by schedule and qualified at `snapshot_837754c3…` (2026-09-24), the first scheduled snapshot: every cell of its five tables equals the audited candidate except the run stamps and the per-hop lineage columns (`supersedes_id` is each continuing row's own id, `identity_predecessors_json` is empty because no id changed against `snapshot_21818212…`; the 52,215 retirements are recorded in that snapshot's tables) (bootstrapped against the five audited parents; details in the execution log): 559,654 rule targets, 462,939 proceedings, 38,406 agenda items, 155,636 agenda–proceeding relationships and 286,770 comment periods, read from dockets `27a2ed4a…`, documents `7907ebe3…`, Federal Register `cf89be63…`, agenda `8a22c704…` and links `9bbeada2…`. Missing FR document references fall from 49,403 to 8,036 (46,945 resolve by unpadding, 1,216 by folding dashes or case, 34 stay ambiguous); labelled docket values join 154,943 link rows where 48,169 did; no evidence, docket or document is lost; 23,959 comment-period ids and 38,034 agenda-relationship ids change and 52,215 proceeding ids retire as predecessors; the Eastern-day rule moves 5,446 first-seen and 73,953 last-seen days one day earlier; all 25 integrity checks are zero, 30 of 30 FR API samples and 10 of 10 changed rows match the raw records. 5,807 unread link rows still name a held docket inside a label or list (887 lists): SpicyDocs 0.31.0's `normalize_docket_references` reads them and is the next adoption (receipts `rulemaking-joins-2026-09-23/candidate-0.31.0/`, `…/scheduled-837754c3/comparison.json`) |

## September 24 report refresh

[Run 36032871739](https://github.com/mikewolfd/spicy-regs/actions/runs/36032871739)
at `cded33d` published generation `52325038…`. The 17:30 UTC anonymous readback
verified its declared schemas and counts: 137 rows describing report parts,
1,757 sections, 133 hearings, 78 hearing–bill links and 269 read checkpoints.
`CRPT-119hrpt494` has parts 1 and 2, and `CRPT-119hrpt811` is keyed to its
`-pt1` source. The receipt is
`workflow-audit-2026-09-24/report-refresh-public-readback.json`.

This generation is **published, with schema/count readback verified; source
qualification pending**. The prior `ef08249e…` audit does not cover the added
packages, multipart identities or new hearing links. Its qualified pins remain
in the table until that audit completes. The new schemas resolved the live
dictionary check's remaining report-field mismatch.

## Open items

The dated narrative behind the rows (the September 21–22 status, the scope gaps and the September 23 continuation) is in the [fork execution log](fork-execution-log-2026-09-23.md); "plan" below is SpicyDocs' `docs/research/consolidation-path-2026-09-22.md`.

- **Newer scheduled generations** await audit (decision 3); `scripts/check_ledger_pins.py` lists the affected rows. `house_communications` still has 1,992 list-only rows at its qualified pin, with 1,000 detail reads per run. The meeting and nomination corrections are published at the pins above.
- **Rulemaking joins:** published (plan A7, A12; ruling 6) and re-run by schedule at `snapshot_837754c3…`. Merged 2026-09-24 for the next scheduled run: labelled docket lists join every docket they name (link rows joined 154,943 → 160,399), and decisions 32–33 (confirmed) remove the Nonrulemaking shells and the notice-built merges — expect proceedings 462,939 → 268,291 with 194,853 ids retired as predecessors (receipt `docket-lists-2026-09-24/published-step/`); audit that snapshot when it lands.
- **Comment and document text:** the derived-text repair is published (plan A6, ruling 7; the 13 attachments whose derived text Mirrulations does not hold are recorded in the receipt); document bodies, extraction status and PDF-extraction evidence are still null (log: Scope and evidence gaps).
- **Other parsing-survey limits:** vote days (A11) are backfilled at `054ec83f…`; `bill_vote_references.date` still holds the UTC instant, so consumers join `vote_id` to `roll_call_votes.vote_day` for the chamber's day. The `bill_subjects` key fix (A5) is pushed; audit its next scheduled generation. Print-citation corrections (A10) are published at the pin above.
- **Comments:** native repair beyond six plus ACF and the dated partition tree remain open. The agency mirror follows local catch-up, resumed after reviewed test exclusions and catalog cleanup; credentials and seeding are complete. At the public checkpoint below, the index advertises more rows than the monolith, so consumer refresh is still pending (see [Operations](#operations-checkpoint) and the [runbook](../etl-catalog-seed.md)).
- **Bill family:** missing bodies and original text, models and historical backfills (log: Scope and evidence gaps, Bill family).
- **Wider populations:** SAM and lobbying beyond their bounded loads; FCC filings before the first window; USAspending beyond its top-100-page selection; CourtListener's full scope, rate budgets, newer catch-up and the non-civil docket scope decision (log: Scope and evidence gaps, Latest continuation).
- **Lifecycles:** a pairing and unknown-docket policy before `rulemaking_lifecycles` returns (decision 4).
- **Multi-part reports:** the migration is published at `52325038…`, and live schema verification passes. Audit the expanded report/hearing population and retained source evidence, including identity preservation, per-part section coverage and the newly nonempty hearing links. The earlier expectation of 120 report rows with unchanged hearings was superseded by this run's measured scope (see [Report refresh](#september-24-report-refresh)).
- **FEC (T05):** broader individual records, committee history and correction streams (log: Scope and evidence gaps).
- **Table III cold start:** a Congress whose first listed act has no OLRC page cannot start its chain (about 45% of acts have none); seed the 120th from the 119th's highest served act before January 2027 — its page is inferred (from 119-1 naming 118-273 as its prior act), not yet observed, to name the next Congress's first act.
- **Operations (T18–T20):** finish local catch-up, verify the public
  mirror and dependent refresh, then qualify the first resumed incremental sweep. The
  [operations checkpoint](#operations-checkpoint) records completed repairs and
  the remaining deployment/access work.

## Operations checkpoint

Public counts checked September 24 at 17:32 UTC; failure state and unchanged
public ETags checked at 17:45 UTC. Catalog recovery was verified at 18:18 UTC
and catch-up restarted at 18:21 UTC. Publication measurements below remain
separate from the source-qualified pins in the output table.

- Catalog bootstrap is complete. The seed check proved every selected docket
  and comment ID present and unique, and verified the original input ETags.
  Manifest `28439568…` was published at 15:35 UTC; later batch publication has
  advanced that checkpoint. Receipts: `etl-seed-2026-09-24/` and
  `ledger-continuation-2026-09-24/`.
- Hosted sweep 36021389999 completed batch 0 and was cancelled at 16:36 UTC
  while batch 1 was staging. The local runner at `cded33d` stopped at 17:42 UTC:
  index validation found missing `docket_id` values on staged comments
  `CFTC-2026-0595-0003` and `CFTC-2026-0595-0005`. The log reports completed
  catalog merges to 279,129 dockets and 23,972,436 comments, followed by failure
  before public upload or manifest save. These are the failed attempt's counts;
  the comment identity check and cleanup below supersede them. Retained staging
  remains in `etl-local-catchup-2026-09-24/output/`, with first-attempt status and
  logs archived in `attempt-01/`. Full ingestion, source qualification, mirror
  publication and readback remain open.
- Source investigation at 17:55–18:00 UTC confirms both affected comments have
  explicit null docket IDs in the mirror JSON, and every mapped native field
  matches staging. They are archived test submissions; the current official API
  returns 404 for both, while their explicit parent document, docket and a
  neighboring comment return 200. Each affected ID appears once in the catalog.
  The application excludes these exact reviewed payloads at `3953175`, with
  raw evidence retained and changed payloads requiring another review. Missing
  docket IDs still refuse admission; staging is validated before persistent
  merges. See the [source investigation](fork-execution-log-2026-09-23.md#september-24-cftc-source-investigation)
  and `cftc-missing-docket-2026-09-24/` receipts.
- Recovery at 18:18 UTC retained the complete catalog rows, removed only the
  two reviewed source versions and verified through a reopened connection:
  23,972,434 comments, every ID unique, neither excluded ID present and no null
  docket IDs. Neither removed row was in the public comments file. The failed
  attempt's local manifest exactly matched the freshly downloaded public
  checkpoint (`db964dd4…` SHA-256). Retry began at 18:21 UTC using `3953175`,
  a fresh `output-retry-02/` directory and that verified manifest. Receipts:
  `cftc-test-exclusion-repair-2026-09-24/`; the
  [repair record](fork-execution-log-2026-09-23.md#september-24-reviewed-test-exclusions-and-catch-up-retry)
  explains the deletion checks and validation.
- Public metadata now shows 279,126 dockets (ETag `91c7ee92…`), 2,001,540
  documents (`549ec0cc…`), and an index (`b2a78582…`) with 113,008 groups
  advertising 23,904,451 comments. The public monolith remains at 23,890,403
  rows (`cce5e386…`), trailing the index by 14,048 rows. The manifest has
  26,190,992 keys (`98336740…`). These are observed publication counts, not
  replacement source-qualified pins. Receipt:
  `workflow-audit-2026-09-24/base-public-readback-during-catchup.json`.
  The public ETags were unchanged after the failure; staging rows and public
  headers are retained in `local-catchup-batch-01-failure.json` in that directory.
- ETL, mirror publication, dedupe and comments monitoring remain paused during
  local catch-up. The completion watcher refused the failed attempt without
  dispatching the mirror or restoring schedules. Its retry requires successful
  batch receipts, CI and the reviewed fork revision, then publishes and verifies
  the mirror and dependent outputs before restoring incremental scheduling.
  `progress.json` and `completion.json` hold current supervisor state;
  `attempt-01/` retains the earlier failure.
- Repairs through `e944365` are pushed and [CI passes](https://github.com/mikewolfd/spicy-regs/actions/runs/36033160292).
  Dedupe is audit-only by schedule; explicit repair preserves its complete
  candidate through interrupted replacement, verified on a real scratch R2
  catalog. Publication now checks retained IDs and exact coverage. Dependent
  jobs wait for inputs, and base ETags are retained and rechecked. The full
  regulatory completion chain still awaits execution. See the
  [workflow review](workflow-review-2026-09-24.md).
- Pages deployment succeeded; active live schemas now pass (below). Wrangler
  verified the active catalog and uncached `r2.dev` endpoint with no custom
  domain; the configured MCP Worker was absent at that check. Purge credentials
  are unnecessary for this serving path. `GEMINI_API_KEY` remains absent and
  Zyte remains configured but unwired. SAM's request repair and the scheduled
  `bill_subjects` repair still await successful-run qualification.
- **Live dictionary verification — resolved:** active published schemas pass [run 36033160021, attempt 2](https://github.com/mikewolfd/spicy-regs/actions/runs/36033160021/attempts/2) after the report migration. The checker follows publication inventory, skips the withdrawn lifecycle object, continues past unreadable tables and fails incomplete verification separately from Pages deployment. This verifies schema agreement; source qualification remains specific to the pins above.
